/**
 * Retired. The testnet/dev-keys caveat now lives only in the docs (Production
 * gate) and the whitepaper, not stamped on every form. Kept as a no-op so
 * existing imports/usages stay valid without a sweep.
 */
export function DevKeysBanner(_props: { compact?: boolean }) {
  return null;
}
