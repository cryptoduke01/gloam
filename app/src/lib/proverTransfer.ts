/**
 * Transfer (private send) witness builder. Now sourced from @gloamtrade/sdk.
 * Re-exported here so existing app imports keep working.
 * See packages/sdk/src/witness.ts.
 */
export { buildTransferWitness } from "@gloamtrade/sdk";
export type { TransferWitness } from "@gloamtrade/sdk";
