/**
 * Transfer (private send) witness builder. Now sourced from @gloam/sdk.
 * Re-exported here so existing app imports keep working.
 * See packages/sdk/src/witness.ts.
 */
export { buildTransferWitness } from "@gloam/sdk";
export type { TransferWitness } from "@gloam/sdk";
