/**
 * Sealed-swap (private trade) witness builder. Now sourced from @gloam/sdk.
 * Re-exported here so existing app imports keep working.
 * See packages/sdk/src/witness.ts.
 */
export { buildSealedSwapWitness, sealedSwapArtifactsReady } from "@gloam/sdk";
export type { SealedSwapWitness } from "@gloam/sdk";
