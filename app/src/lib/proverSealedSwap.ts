/**
 * Sealed-swap (private trade) witness builder. Now sourced from @gloamtrade/sdk.
 * Re-exported here so existing app imports keep working.
 * See packages/sdk/src/witness.ts.
 */
export { buildSealedSwapWitness, sealedSwapArtifactsReady } from "@gloamtrade/sdk";
export type { SealedSwapWitness } from "@gloamtrade/sdk";
